# Production API base URL — used by `expo export` / EAS builds (mode=production).
# Local `expo start` keeps auto-detecting your dev machine's LAN IP (see
# src/api/client.ts) unless .env also sets EXPO_PUBLIC_API_URL.
EXPO_PUBLIC_API_URL=https://lavenderblush-stinkbug-399468.hostingersite.com/api/v1
