# Production API base URL — used by `expo export` / EAS builds (mode=production).
# Local `expo start` keeps auto-detecting your Mac's LAN IP (see src/api/client.ts),
# so this does not affect development.
EXPO_PUBLIC_API_URL=http://172.20.10.8:8000/api/v1
