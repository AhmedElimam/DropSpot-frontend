import { SupportCenter } from '@/components/support/SupportCenter';

/** Parent → platform admin. Reached from the profile's support card. */
export default function ParentSupport() {
  return <SupportCenter title="مركز الحلول" />;
}
