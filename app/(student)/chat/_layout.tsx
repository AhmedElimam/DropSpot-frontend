import { Stack } from 'expo-router';

/** Course chat: the rooms list and one room, stacked inside the student tabs. */
export default function StudentChatLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
