import { SupportCenter } from '@/components/support/SupportCenter';

/** Student → platform admin. Reached from the profile's support card. */
export default function StudentSupport() {
  return <SupportCenter title="مركز الحلول" />;
}
